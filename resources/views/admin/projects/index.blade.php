@extends('layouts.admin')
@section('content')
<x-page-header title="CentralPanels" description="Recherchez une instance, son propriétaire et son état de déploiement." eyebrow="Administration"/>
<form class="mb-6 flex flex-col gap-3 sm:flex-row" method="GET"><input class="field sm:max-w-sm" name="q" value="{{ request('q') }}" placeholder="Nom ou UUID" aria-label="Nom ou UUID"><input class="field sm:max-w-xs" name="status" value="{{ request('status') }}" placeholder="Statut" aria-label="Statut"><x-button variant="secondary">Filtrer</x-button></form>
<x-table><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800/60 dark:text-slate-400"><tr>@foreach(['Projet','Utilisateur','Plan','Statut','Node','Hostname','Date'] as $h)<th scope="col" class="px-5 py-3 text-left font-semibold">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@foreach($projects as $p)<tr class="hover:bg-slate-50/70 dark:hover:bg-slate-800/40"><td class="px-5 py-4"><a class="font-medium hover:text-brand-700 dark:hover:text-brand-300" href="{{ route('admin.projects.show',$p->uuid) }}">{{ $p->name }}</a></td><td class="px-5 py-4">{{ $p->owner->email }}</td><td class="px-5 py-4">{{ $p->plan->name }}</td><td class="px-5 py-4"><x-status-badge :status="$p->status"/></td><td class="px-5 py-4">{{ $p->deployment?->node?->name ?? '—' }}</td><td class="px-5 py-4">{{ $p->publicHostname() ?? '—' }}</td><td class="px-5 py-4 tabular-nums">{{ $p->created_at->format('d/m/Y') }}</td></tr>@endforeach</tbody></x-table><div class="mt-6">{{ $projects->links() }}</div>
@endsection
