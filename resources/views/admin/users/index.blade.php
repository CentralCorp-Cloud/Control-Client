@extends('layouts.admin') @section('content')<h1 class="text-2xl font-bold">Utilisateurs</h1><form class="my-6 flex gap-3"><input class="field max-w-sm" name="q" value="{{ request('q') }}" placeholder="Nom, email ou UUID"><x-button variant="secondary">Rechercher</x-button></form><x-table><thead><tr>@foreach(['Utilisateur','Rôle','Statut','Email vérifié','CentralPanel','Inscription'] as $h)<th class="px-4 py-3 text-left">{{ $h }}</th>@endforeach</tr></thead><tbody>@foreach($users as $user)<tr class="border-t border-slate-200 dark:border-slate-800"><td class="px-4 py-3"><a class="font-medium text-brand-600" href="{{ route('admin.users.show',$user->uuid) }}">{{ $user->name }}</a><div class="text-xs text-slate-500">{{ $user->email }}</div></td><td class="px-4 py-3"><x-status-badge :status="$user->role"/></td><td class="px-4 py-3"><x-status-badge :status="$user->status"/></td><td class="px-4 py-3">{{ $user->email_verified_at?'Oui':'Non' }}</td><td class="px-4 py-3">{{ $user->projects_count }}</td><td class="px-4 py-3">{{ $user->created_at->format('d/m/Y') }}</td></tr>@endforeach</tbody></x-table><div class="mt-6">{{ $users->links() }}</div>@endsection
