@extends('layouts.admin') @section('content')<div class="mx-auto max-w-3xl"><h1 class="mb-6 text-2xl font-bold">Paramètres Control Plane</h1><x-alert type="warning">Les secrets Stripe et les jetons Agent propres à chaque Node ne sont jamais affichés ici. Le suffixe des panels est défini par <code>CENTRALPANEL_DOMAIN_SUFFIX</code>.</x-alert><x-card><form method="POST" action="{{ route('admin.settings.update') }}" class="space-y-4">@csrf @method('PUT')@foreach(['registration_open'=>'Inscriptions ouvertes','global_maintenance'=>'Maintenance générale','scheduler_ram_margin'=>'Marge RAM scheduler','scheduler_disk_margin'=>'Marge disque scheduler','payment_grace_days'=>'Délai impayé (jours)','retention_days'=>'Rétention avant purge manuelle','default_panel_version'=>'Version CentralPanel par défaut'] as $key=>$label)<x-input :label="$label" name="settings[{{ $key }}]" :value="$settings->get($key)?->value"/>@endforeach<x-button>Enregistrer</x-button></form></x-card></div>@endsection
