@extends('layouts.admin') @section('content')<h1 class="text-2xl font-bold">Agent Operations</h1><form class="my-6 flex gap-3"><input class="field max-w-xs" name="status" value="{{ request('status') }}" placeholder="Status"><input class="field max-w-xs" name="type" value="{{ request('type') }}" placeholder="Type"><x-button variant="secondary">Filtrer</x-button></form><x-table><thead><tr>@foreach(['Operation','Type','Project','Utilisateur','Node','Status','Started','Correlation ID'] as $h)<th class="px-4 py-3 text-left">{{ $h }}</th>@endforeach</tr></thead><tbody>@foreach($operations as $op)<tr class="border-t dark:border-slate-800"><td class="px-4 py-3"><a class="text-brand-600" href="{{ route('admin.operations.show',$op->uuid) }}">{{ Str::limit($op->uuid,13) }}</a></td><td class="px-4 py-3">{{ $op->type }}</td><td class="px-4 py-3">{{ $op->deployment->project->name }}</td><td class="px-4 py-3">{{ $op->deployment->project->owner->email }}</td><td class="px-4 py-3">{{ $op->node->name }}</td><td class="px-4 py-3"><x-status-badge :status="$op->status"/></td><td class="px-4 py-3">{{ $op->started_at?->format('d/m H:i') ?? '—' }}</td><td class="px-4 py-3 font-mono text-xs">{{ $op->correlation_id }}</td></tr>@endforeach</tbody></x-table><div class="mt-6">{{ $operations->links() }}</div>@endsection
