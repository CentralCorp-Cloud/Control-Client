@extends('layouts.admin') @section('content')<h1 class="text-2xl font-bold">Audit Logs</h1><form class="my-6 flex gap-3"><input class="field max-w-sm" name="q" value="{{ request('q') }}" placeholder="Action"><x-button variant="secondary">Rechercher</x-button></form><x-table><thead><tr>@foreach(['Date','Actor','Action','Cible','IP','Metadata'] as $h)<th class="px-4 py-3 text-left">{{ $h }}</th>@endforeach</tr></thead><tbody>@foreach($logs as $log)<tr class="border-t align-top dark:border-slate-800"><td class="px-4 py-3">{{ $log->created_at->format('d/m/Y H:i:s') }}</td><td class="px-4 py-3">{{ $log->actor_id ?? 'Système' }}</td><td class="px-4 py-3 font-medium">{{ $log->action }}</td><td class="px-4 py-3">{{ $log->target_type }} #{{ $log->target_id }}</td><td class="px-4 py-3">{{ $log->ip_address }}</td><td class="max-w-xs px-4 py-3 font-mono text-xs">{{ json_encode($log->metadata,JSON_UNESCAPED_UNICODE) }}</td></tr>@endforeach</tbody></x-table><div class="mt-6">{{ $logs->links() }}</div>@endsection
