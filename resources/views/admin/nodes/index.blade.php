@extends('layouts.admin') @section('content')<div class="mb-6 flex justify-between"><h1 class="text-2xl font-bold">Nodes</h1><x-button href="{{ route('admin.nodes.create') }}">Ajouter un Node</x-button></div><x-table><thead><tr>@foreach(['Node','Status','Scheduling','Version','Domaines custom','CPU','RAM disponible','Disque disponible','Deployments','Last seen'] as $h)<th class="px-4 py-3 text-left">{{ $h }}</th>@endforeach</tr></thead><tbody>@foreach($nodes as $n)<tr class="border-t border-slate-200 dark:border-slate-800"><td class="px-4 py-3"><a class="font-medium text-brand-600" href="{{ route('admin.nodes.show',$n->uuid) }}">{{ $n->name }}</a><div class="text-xs text-slate-500">{{ $n->region }}</div></td><td class="px-4 py-3"><x-status-badge :status="$n->status"/></td><td class="px-4 py-3">{{ $n->scheduling_enabled?'Actif':'Désactivé' }}</td><td class="px-4 py-3">{{ $n->agent_version ?? '—' }}</td><td class="px-4 py-3">{{ $n->supports('hostname_aliases')?'Oui':'Non' }}</td><td class="px-4 py-3">{{ $n->cpu_count }}</td><td class="px-4 py-3">{{ round($n->memory_available_bytes/1073741824,1) }} Gio</td><td class="px-4 py-3">{{ round($n->disk_available_bytes/1073741824,1) }} Gio</td><td class="px-4 py-3">{{ $n->deployment_count }}</td><td class="px-4 py-3">{{ $n->last_seen_at?->diffForHumans() ?? 'Jamais' }}</td></tr>@endforeach</tbody></x-table><div class="mt-6">{{ $nodes->links() }}</div>@endsection
