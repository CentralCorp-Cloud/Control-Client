@extends('layouts.dashboard')
@section('content')
<x-page-header title="Notifications" description="Suivez les événements importants liés à vos instances et à votre compte."><x-slot:actions><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<x-button variant="secondary">Tout marquer comme lu</x-button></form></x-slot:actions></x-page-header>
@if($notifications->isNotEmpty())<div class="surface divider-list">@foreach($notifications as $notification)<article class="flex gap-4 p-4 sm:p-5 {{ $notification->read_at?'opacity-70':'' }}"><span class="mt-0.5 flex size-9 shrink-0 items-center justify-center rounded-lg {{ $notification->read_at?'bg-slate-100 text-slate-500 dark:bg-slate-800':'bg-brand-50 text-brand-700 dark:bg-brand-950 dark:text-brand-300' }}"><x-icon name="bell" class="size-4"/></span><div class="min-w-0 flex-1"><div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><h2 class="text-sm font-semibold">{{ $notification->data['title'] ?? 'Notification CentralCloud' }}</h2><p class="mt-1 text-sm leading-6 text-slate-500 dark:text-slate-400">{{ $notification->data['message'] ?? '' }}</p></div>@unless($notification->read_at)<form method="POST" action="{{ route('notifications.read',$notification->id) }}">@csrf<button class="min-h-11 text-sm font-medium text-brand-700 hover:underline dark:text-brand-300">Marquer comme lue</button></form>@endunless</div><p class="mt-2 text-xs text-slate-400">{{ $notification->created_at->diffForHumans() }}</p></div></article>@endforeach</div>@else<x-empty-state title="Aucune notification" description="Les informations importantes concernant vos CentralPanel apparaîtront ici."/>@endif<div class="mt-6">{{ $notifications->links() }}</div>
@endsection
